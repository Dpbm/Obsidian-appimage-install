# install Obsidian.AppImage as an app

1. download the `AppImage file` to your home directory (you can find it [here](https://obsidian.md/download))
2. run:

```bash
chmod +x install.sh
./install.sh
```

3. enjoy :)
